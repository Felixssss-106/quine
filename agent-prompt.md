# Quine · 开发启动提示词（给实现 Agent 的完整简报）· v2

> 版本 2026-09-17 · **v2 取代 v1**（v1 结构保留；v2 增补：完整架构与模块图、数据模型、工具协议、验收清单、像素图标定稿与"设置内图标切换"、仓库信息、更细的里程碑 DoD）
> 用法：把本简报全文交给任何编码 Agent（Codex / Claude Code / Cursor / 任意本地 Agent）作为项目启动上下文；连同 `docs/` 设计文档集一起使用。
> **事实来源优先级：`docs/` 设计文档集 > 本简报转述**；两者未覆盖处，先问人类，不得自行发明。

---

## 0. 你的角色与工作方式（先读这一节）

你是「Quine」的实现 Agent。人类是产品与设计主导者（业余时间投入），你负责实现。

### 0.1 工作规则

1. **先对齐，再动工。** 动工前完整阅读本简报 + `docs/` 设计文档集（附录 A），然后输出《理解与计划》：
   - ① 建议的仓库结构与模块图
   - ② 当前里程碑的 Issue 级拆解（每项含验收标准）
   - ③ 需要人类确认的问题清单（按优先级）
   得到人类确认后，才开始写代码。
2. **设计文档是唯一事实来源**。与简报冲突时以设计文档为准；文档未覆盖处先问。
3. **垂直切片交付**。每个里程碑结束时必须得到一个"能在手机上干成一件真事"的可运行版本。`main` 永远保持可构建。
4. **不确定时选择可逆方案**（能回滚的架构、能替换的实现、能开关的功能），并在 CHANGELOG 里记录选择。
5. **每个工作会话**：开始时读 `CHANGELOG.md` 与相关设计文档；结束时更新 `CHANGELOG.md`（做了什么 / 为什么 / 怎么验证）。

### 0.2 纪律红线（违反即回退重做）

- **不静默失败**：任何失败必须有可见反馈（界面 + 通知）
- **危险操作最小拦截不可移除**：删数据 / 花钱 / 发消息三类默认必拦
- **文件改动前必须自动快照，且可一键回滚**
- **API Key 只存本机（Android Keystore 加密）**；日志严禁包含 Key、对话内容明文
- **「状态永不丢」**：旋转 / 折叠 / 进程重启后，任务、草稿、滚动位置可恢复
- **动效全站只有两套弹簧、三档时长**（见 §5.6）；必须响应系统"移除动画"
- **「神来之笔」全局限额 2 处**，不得私自增加

### 0.3 语言与文案

- 与人交流：中文；代码 / 注释 / 提交信息：英文
- UI 文案遵循「文案基调」（§4.7）：人话、短句、克制；每个错误文案必须带下一步动作

---

## 1. 项目是什么

**一句话**：Quine 是一个**安卓独占的本地 Agent 宿主应用**——"手机上的 Codex / Claude Code"。能改文件、搜网、操控系统、执行命令、跑自动化；全部能力在设备本地运行（proot Linux 沙箱），**只有模型推理出网**。

| 项 | 现状 |
|---|---|
| 名字 | **Quine**（读"奎因"；源自"能输出自己源代码的程序"） |
| 仓库 | `https://github.com/Felixssss-106/quine`（2026-09-17 创建，**当前为空仓**；文档与代码的首次推送时机由人类决定） |
| 图标 | **像素风 `>_`**（16×16 网格；`>` 墨黑/纯白 + `_` 矢车菊蓝 `#6495ED`）；**浅色（白底）与深色（黑底）两版，用户在 App 设置中自行切换** → 资产见 `icons/pixel-final/` |
| 目标用户 | 极客 / 开发者优先（自带 API Key，BYO key）；先自用跑顺，再外化 |
| 发行 | 开源，GitHub 首发（许可证待定，见附录 B） |
| 设计哲学 | **透明（看得见它干活）· 可控（分级信任）· 状态永不丢** |
| 差异化 | ① 手机原生深度操控（Shizuku / 无障碍）② 常驻自动化（定时 / 事件触发的自主任务）；美术与体验是贯穿性验收标准 |

---

## 2. 技术架构（已定案，不得擅自更换）

### 2.1 技术选型总表

| 模块 | 决定 | 关键点 |
|---|---|---|
| 平台 | Android 独占（手机 / 平板 / 折叠屏） | minSdk 建议 26+，实现前与人类确认 |
| 客户端 | Kotlin + Jetpack Compose，单代码库 | 单 Activity + Navigation；MVVM（ViewModel + StateFlow）；Gradle 模块化 + Version Catalog |
| 沙箱 | **proot 自包含** | 自带 rootfs（发行版见附录 B）；首启下载/解压；不依赖 Termux |
| 权限抽象 | 四档：标准 API / Shizuku / Root（v1 后置）/ 无障碍 | 统一 `系统能力` 接口，按可用性逐级升级 |
| 模型网关 | **自研**：OpenAI 兼容为主轴 + Anthropic / Gemini 原生适配器 | 流式统一；BYO key；baseUrl 可覆盖 |
| Agent loop | **全自研**：状态机 + 工具调用 + 流式渲染 | 不用现成框架 |
| 工具扩展 | 内置原生工具 + **MCP 兼容层**（本地 stdio / 远程 HTTP） | 不做内置插件市场 |
| 记忆 | 透明文件式（可看可编）+ 会话持久化 | 为向量/RAG 预留结构 |
| 设计系统 | Material 3 Expressive 打底 + 自研「双面人」token 层 | 不引入第三方设计系统 |
| 动效栈 | Compose 原生动画 + material-motion-compose + Rive + compose-shimmer | Lottie 仅素材补充 |
| 场景库 | sora-editor（编辑/diff）· multiplatform-markdown-renderer（消息）· Vico（图表）· Reorderable（拖拽）· Balloon（气泡）· Haze（毛玻璃，仅浮层/栏位） | 见 design-refs.md |
| 字体 / 图标 | 等宽 = Maple Mono；图标 = Lucide + Material Symbols；正文 = 系统字体（中文字体配对后置） | |
| 存储 | DataStore（设置）+ Room（会话/任务/自动化）+ 私有目录（快照）+ Keystore（密钥） | |

### 2.2 模块结构（建议，可调整后经人类确认）

```
app/                  # 单 Activity、导航、主题、全局状态
feature-chat/         # 聊天面（消息流、composer、活动卡）
feature-tasks/        # 任务面（列表、详情时间线、五件套渲染）
feature-onboarding/   # 首启两屏
feature-settings/     # 设置（供应商/权限/自动化/MCP/外观[含图标切换]/存储/关于）
core-loop/            # agent 状态机与编排
core-gateway/         # 模型网关（providers、SSE、计费统计）
core-tools/           # 内置工具定义与执行器
core-sandbox/         # proot 沙箱生命周期与通道
core-storage/         # Room/DataStore/快照
core-design/          # design tokens、通用组件
core-common/          # 工具函数、结果类型、错误模型
docs/                 # 设计文档（见附录 A）
icons/                # 图标资产（pixel-final/）
```

依赖规则：`app → feature-* → core-*`；`core` 之间单向依赖、不互相成环；`feature` 之间不直接依赖。

### 2.3 Agent Loop 状态机（核心）

状态：`Idle → Planning → ToolCall → ToolResult → …（循环）… → Streaming → WaitingApproval → Done / Failed / Cancelled`

- **流式**：模型输出按块进入 UI 通道（`Flow<LoopEvent>`），UI 只做渲染；每块 80ms 节奏、光标 1s 呼吸
- **工具调用**：解析 → 校验参数（JSON Schema）→ 权限检查（§2.7）→ 风险分级 → 执行 → 结果入上下文；工具结果 > 上下文预算时自动截断并保留引用
- **取消语义**：任何状态可取消；沙箱进程 kill；已产生文件改动保留快照引用
- **重试预算**：单步最多 2 次自动重试（网络/瞬态错误），超限 → 换策略 1 次 → 仍失败则升级给用户（人话三段：发生了什么 / 影响 / 你可以怎么办）
- **审批暂停**：进入 `WaitingApproval` 时 UI 出确认条 + 通知；批准/拒绝后继续或终止
- **成本统计**：每步记录 token 与费用（tabular 数字展示）
- **上下文管理**：滑动窗口（近 N 轮全量）+ 会话摘要（早前内容压缩）+ 记忆文件注入（§3.8）；预算表在实现期与人类定

### 2.4 模型网关

- `interface LlmProvider`：`stream(messages, tools, options): Flow<LlmEvent>`
- 适配器：`OpenAiCompatProvider`（主轴，覆盖 DeepSeek/Kimi/GLM/Qwen 等）、`AnthropicProvider`、`GeminiProvider`
- SSE 解析、增量工具调用拼装、usage 统计、错误映射：`网络 / 鉴权 / 限流 / 内容策略 / 服务端` → 统一 `LlmError` + 文案
- Key 管理：多供应商多 Key；Keystore 加密；不落日志；设置页可测连通性

### 2.5 沙箱（proot）

- 生命周期：首启初始化（下载/解压 rootfs → 校验 → 就绪）→ 启动 proot 会话 → 常驻通道（stdin/stdout 流）
- 文件通道：沙箱路径 ↔ 应用私有目录映射；与"用户授权目录"（SAF）双向同步策略（复制 / 绑定）
- 进程管理：会话隔离；超时与 kill；后台任务进程保护
- 性能：冷启动时间预算、命令 I/O 流式回显（终端块"永久暗面"渲染）

### 2.6 数据模型（草案）

- `Conversation(id, title, createdAt, updatedAt, pinned)`
- `Message(id, convId, role, contentJson, toolCallsJson, usageJson, createdAt)`
- `TaskRun(id, title, status, originType(conv/timer/share), originConvId?, createdAt, startedAt, endedAt, costJson, errorJson?)`
- `TaskStep(id, taskId, idx, type(think/tool/output/error/approval), title, payloadJson, ts)`
- `Snapshot(id, path, blobRef, taskId?, createdAt)`（内容寻址存储）
- `Automation(id, name, triggerJson(time/event), prompt, enabled, lastRunAt, lastResult)`
- `PermissionGrant(id, kind(dir/setting/app/action), target, scope(once/session/always), createdAt)`

### 2.7 权限与安全

- 能力等级枚举：`STANDARD / SHIZUKU / ROOT(后置) / ACCESSIBILITY`——运行时检测可用性，UI 显示当前档位
- 危险等级分类（决定是否拦截）：`安全(沙箱内) / 敏感(设置·通知·剪贴板) / 高危(删数据·花钱·发消息·装卸应用)`
- 审批矩阵（三档信任 × 危险级）：
  - **保守**：所有设备层操作确认
  - **标准**（默认）：沙箱内免确认；设备层首次确认、可记住；高危永远确认
  - **狂奔**：其余全自动；高危默认仍拦，设置内可显式关闭（红色警告）
- 安全清单：Keystore、日志脱敏、快照强制、组件导出最小化、WebView 安全配置（禁 file 访问、只允许必要域名）

---

## 3. 功能规格 v1（全量 + 验收标准）

### 3.1 文件
- **能力**：读 / 写 / 移动 / 删除 / 搜索（沙箱内 ripgrep）/ diff 展示 / 导出分享
- **范围**：默认 SAF 用户授权目录（沙箱工作区 + 授权域）；Shizuku 开启后可选"全盘模式"（进阶开关）
- **红线**：任何写入前自动快照；一键回滚；批量改动先给 diff 预览
- **验收**：在授权目录新建/修改/删除文件；改坏一键回滚；批量修改有预览且可取消

### 3.2 搜索与浏览器
- **眼睛**：多后端可插拔搜索（免 key 兜底 + Tavily / Brave / Exa 可配置）+ 网页正文抓取（阅读视图）
- **手**：内置**可控 WebView**——导航 / 点击 / 输入 / 抓取；登录态在 App 内
- **明确不碰**：系统浏览器（脆弱、风控、维护无底洞）
- **验收**：问一个需要联网的问题 → 给出带来源 chip 的回答 → 点来源可打开阅读视图；（M2）在内置 WebView 完成一次搜索+打开+摘录

### 3.3 系统操控（按层解锁）
- **标准 API**：通知、剪贴板、日历、闹钟、媒体控制
- **Shizuku**：应用管理（装/卸/冻结/清缓存）、设置开关、force-stop、shell
- **无障碍（冲刺层，默认关闭）**：读屏 + 模拟操作；第一条能力线 = **跨 App 自动化（自然语言探索式）**；配套：屏幕轨迹可视化、高风险点停顿确认、失败自恢复与重试预算
- **v1 优先场景**：设置开关 / 应用管理 / 跨 App 自动化
- **验收**：一句话开启勿扰；Shizuku 下卸载一个测试应用；（M4）完成一次"帮我签到"类跨 App 流程，或给出清晰失败点与轨迹

### 3.4 命令执行
- proot 完整 Linux 沙箱（单发行版、开箱即用）；Root 设备可选 chroot 提速（后置）
- 终端流实时可见（任务详情页，等宽 + 永久暗面）
- **验收**：让沙箱跑一段 python 脚本并把结果文件取回授权目录

### 3.5 自动化
- **触发**：定时（自然语言创建："每天 9 点…"）+ 少量高价值事件（充电 / 指定 WiFi / 开机）
- **红线**：前台服务常驻 + 电池优化白名单引导 + watchdog + 失败必通知；任务队列持久化
- **验收**：创建定时任务；重启手机后任务仍在、到点执行并通知；失败必通知

### 3.6 语音与视觉（附加）
- 语音输入（长按说话 → 实时转写 80ms/块）；图片/截图理解（轻量：截图给模型看）
- **验收**：语音输入一段话并正确上屏；分享一张截图进来，模型能描述并处理

### 3.7 移动入口
- 分享进来（share sheet：文本/图片/文件）+ 通知内快捷操作（完成→查看 / 需确认→就地批准 / 失败→重试）
- 小组件 / 快捷磁贴：后置
- **验收**：从任意 App 分享一段文本进来，能直接发起任务

### 3.8 记忆与扩展
- 记忆：透明文件式（`memory/` 目录：全局笔记 + 项目笔记；可看可编）；会话持久化
- 扩展：内置工具 + **MCP 兼容层**（本地 stdio / 远程 HTTP）；设置页管理 MCP 服务器
- **验收**：编辑记忆文件后，新会话能引用其中信息；接入一个 MCP server 并能调用其工具

### 3.9 内置工具清单（草案，实现期细化 schema）

| 工具 | 说明 | 权限级 |
|---|---|---|
| `fs_read / fs_write / fs_list / fs_search / fs_diff` | 文件操作族 | STANDARD（授权域内） |
| `shell_run` | 沙箱执行命令 | STANDARD（沙箱内） |
| `web_search / web_fetch` | 搜索与抓取 | STANDARD |
| `webview_*` | 内置浏览器操控 | STANDARD |
| `sys_settings / sys_notify / sys_clipboard` | 系统设置与通知 | SENSITIVE（确认后） |
| `app_manage` | 应用管理 | SENSITIVE（Shizuku） |
| `task_schedule / task_status` | 自动化 | STANDARD |
| `screen_read / screen_action` | 读屏与操作（M4） | ACCESSIBILITY |
| `memory_write` | 写入记忆文件 | STANDARD |
| `mcp_call` | 调用外部 MCP 工具 | 按声明 |

### 3.10 v1 明确不做
云同步 / 多设备；内置插件市场；多 agent 协作；桌面端；iOS；双向语音对话；折叠悬停 / 桌面窗口模式（后置不排期）。

---

## 4. 交互与信息架构（实现细节见 `docs/page-specs.md`）

### 4.1 屏幕清单（v1）

| 屏 | 说明 |
|---|---|
| 首启·欢迎 | 「你好。」+ 定位（"文件不出这台设备"）+ 模型 Key 接入 |
| 首启·沙箱初始化 | 四步清单 + 等宽日志缓滚 + 细进度条；失败态带重试/清理空间 |
| 聊天 | 消息流（用户=灰气泡 / AI=裸文本）、活动卡、composer、快捷 chips |
| 任务列表 | 运行中 / 已完成 / 失败分组；拖拽排序 |
| 任务详情 | **单一时间线 + 五件套内联**（见 §4.3）+ 产物区 + 粘底操作条 |
| 工作区（横屏/平板） | 聊天栏 + 工作区双栏：终端 / 文件 / diff 分页 |
| 设置 | 供应商 / 权限 / 自动化 / MCP / 记忆 / 外观（含**应用图标切换**）/ 存储 / 关于 |

### 4.2 透明呈现
- 进行中：聊天流里"活动卡"（一行摘要，可展开）；结束后自动折叠
- 全局"安静 ↔ 啰嗦"滑杆；任务详情页 = 信息密集模式
- 条目一律**人话标题**（「改了 3 个文件」），不许术语裸奔；顶部过滤 chips（全部/文件/终端/网页/屏幕）

### 4.3 任务详情五件套（内联附件块）
① 时间线（骨）② diff 块（等宽；新增绿 #1A7F37 / 删除红 #CF222E（浅）、#3FB950 / #F85149（深），`+/−` 双通道保底）③ 终端块（等宽 + 永久暗面）④ 网页块（favicon+标题+域名+时间 → 抽屉阅读视图）⑤ 屏幕块（关键帧横排 + 动作说明 → 回放）

### 4.4 信任模型
三档（保守/标准/狂奔），细节见 §2.7 审批矩阵。确认弹层必须讲人话三段：**要做什么 + 影响 + 能否撤销**。

### 4.5 通知
三节点：完成 / 失败 / 需确认；通知可直接操作；前台服务=低打扰"心跳条"。

### 4.6 composer
黑圆钮三态：空=语音 → 有字=↑发送 → 生成中=▪停止；档位胶囊常驻（保守/标准/狂奔）；模式 chips 可增删；生成超 ~2 分钟浮出「要转成后台任务吗？」；发送后立即复位不失焦。

### 4.7 文案基调
人话、短句、克制；不卖萌不谄媚；称呼用「你」；**每个错误文案必须带下一步动作**；术语不硬翻译。

### 4.8 设置 · 应用图标（2026-09-17 新增）
- 位置：设置 → 外观 →「应用图标」
- 选项：**浅色**（白底 `>_`）/ **深色**（黑底 `>_`）单选；默认浅色；选中 = ink 勾
- 小字：「切换后桌面图标会变化，个别机型需等几秒」
- 实现：`activity-alias` 双启动入口 + `setComponentEnabledSetting`；切换可能弹回桌面（系统行为）
- 详见 `icons/pixel-final/README.md`

---

## 5. 视觉与动效（数值基准见 `docs/visual-spec.md`、`docs/motion-spec.md`）

### 5.1 气质
**竖屏 = ChatGPT 式亲和对话面；横屏 = Codex 式工程台面**（"转个方向，从聊天切到工程台"）。

### 5.2 视觉纪律（六条）
1. 黑白灰是主角，颜色是客人：彩色只允许出现在 **链接 / 聚焦 / 危险 / diff 语义** 四处
2. 无渐变、无重投影；层级靠留白、字重、毛发分隔线
3. 圆角即语言：按钮=药丸(999) · 卡片=16 · 抽屉=20
4. 动效服务「完整/优雅/流畅」
5. 双主题=镜像逻辑：跟随系统，两套均精做
6. 「神来之笔」全局限额 2 处

### 5.3 色 token（基准）
- 浅色：ink `#0D0D0D` / bg `#FFFFFF` / fill `#F4F4F5` / line `#E8E8EA` / accent `#007AFF` / danger `#FF3B30`
- 深色：ink 翻转 `#FFFFFF` / bg `#000000` / fill `#1C1C1E` / line `#2C2C2E` / accent `#0A84FF`
- diff：`#1A7F37` / `#CF222E`（浅），`#3FB950` / `#F85149`（深）

### 5.4 字体
- 等宽 = **Maple Mono**（代码/终端/diff/数据）；正文 = 系统字体（中文字体配对见附录 B）
- 字阶：Display 36 → Caption 13；Mono 三档 14/13/12；tabular 数字
- 中文纪律：字重降一档、行高 +2、全角标点、中英混排补 0.25em

### 5.5 网格与形状
4pt 网格；页面边距 20；列表行高 ≥56；按钮高 52–56；chips 高 32–36。

### 5.6 动效令牌
- 时长三档：120 / 240 / 400 ms
- 弹簧唯二：标准（damping 0.85 / stiffness 380）、柔和（0.95 / 200）
- 触感：轻点 light · 完成 medium · 错误 heavy×2 · 危险确认 double tick
- 「无跳变表」全部满足；reduced-motion 降级分支；120Hz 适配
- 两个"神来之笔"：① agent 工作态字效（噪点/故障，小面积）② 完成收束（进度点汇拢 → 黑白反转 120ms → 回落）

### 5.7 图标
`icons/pixel-final/`：像素 `>_`（16×16；`>` 墨黑/纯白 + `_` `#6495ED`）；浅/深两版由用户在设置内切换；含 48–1024 全尺寸、自适应三层（背景色 / 前景层 / 单色层）。规范见该目录 README.md。

---

## 6. 里程碑（垂直切片 · 业余节奏）与 DoD

> **每个里程碑的 DoD（Definition of Done）**：① 可构建、可安装、可日用 ② 新增功能有测试（核心逻辑） ③ `CHANGELOG.md` 更新 ④ 一张截图进 `docs/shots/` ⑤ 至少一次真实自用记录

- **M0 骨架**：仓库初始化 + 单会话聊天 UI + 模型网关（OpenAI 兼容）+ 最小 loop + 1 个工具（读文件）
  验收：能对话、看流式、补全一个工具调用并回显结果
- **M1 底座**：proot 沙箱 + 文件工具（快照/回滚）+ 命令执行 + 双主面（聊天/任务）+ diff/终端查看
  验收："手机上的 Codex lite"——改真文件、跑命令、可回滚；**自己每天能用**
- **M2 代理 A**：搜索/抓取 + 内置 WebView 操控 + 通知体系（三节点 + 快捷操作）
- **M3 代理 B**：Shizuku 系统操控（设置开关/应用管理）+ 定时自动化（前台服务 + watchdog + 白名单引导）
- **M4 冲刺**：无障碍跨 App 自动化（探索式 + 屏幕轨迹）+ 语音输入 + 视觉理解
- **M5 打磨发布**：深浅主题精修 / 动效 / 性能 / 文档 / 开源发布（GitHub Releases）

每阶段纪律：功能开关 · 失败可见 · 阶段结束砍一轮范围。

---

## 7. 工程约定

- **仓库**：`https://github.com/Felixssss-106/quine`（当前为空仓）。首个提交建议包含：本提示词 + `docs/` 全套 + `icons/` + `README.md` + 脚手架（时机由人类定）
- **文档**：`docs/` 是事实来源；每里程碑更新 `CHANGELOG.md` 与截图
- **Git**：main 分支保护；feature 分支 + 自审 PR；Conventional Commits
- **测试**：核心（loop/网关/工具协议）单测必须；关键流程仪器测试；发版前人工狗粮清单（附录 C）
- **CI**：GitHub Actions（lint + test + assembleDebug）
- **安全**：Keystore / 日志脱敏 / 组件导出最小化 / 快照强制（对照 §2.7 清单逐条过）
- **性能**：列表用 Lazy + animateItem；流式渲染与动画解耦；启动时间与内存预算实现期定
- **无障碍**：TalkBack 标签齐全；响应 reduce-motion；中文优先文案
- **图标切换**：`activity-alias` 双入口 + `setComponentEnabledSetting`；UI 提示启动器刷新延迟
- **发布**：GitHub Releases（tag + APK + changelog）；酷安 / F-Droid 顺延；Play 后置评估

---

## 8. 名词表（全队同一语言）

双面人（竖屏对话/横屏工程台）· 五件套（时间线·diff·终端流·网页轨迹·屏幕轨迹）· 档位（保守/标准/狂奔）· 心跳条 · 活动卡 · 呼吸点 · 神来之笔（限额 2）· 图标记（设置内深浅切换）· Quine。

---

## 9. 你的第一条任务（照做）

1. 通读本简报 + 附录 A 全部文档。
2. 输出《理解与计划》：
   - ① 仓库结构与模块图（含依赖关系）
   - ② M0 的 Issue 级拆解（标题 / 验收标准 / 预估）
   - ③ 开放问题清单（至少覆盖附录 B）
3. **等人类确认后再开始编码。** 首个提交内容见 §7 约定。

---

## 附录 A · 设计文档集（随仓库放入 `docs/`）

| 文件 | 内容 |
|---|---|
| `v1-blueprint.md` | 总蓝图：定位 / 能力边界 / 技术 / 交互 / 美术适配 / 里程碑 / 风险 / 待定项 |
| `visual-spec.md` | 视觉规格：色板 token、字阶、圆角/网格、组件清单、双主题检查表 |
| `motion-spec.md` | 动效规格：令牌、10 组关键转场、无跳变表、Compose 映射、验收 |
| `page-specs.md` | 页面规格：首启 / 任务详情 / composer / 设置·应用图标 |
| `design-refs.md` | 设计资源选型：库清单与淘汰理由、灵感站、字体图标 |
| `icons/pixel-final/README.md` | 图标资产：构成、色值、文件清单、自适应规范、切换实现 |
| `agent-prompt.md` | 本文件（v2） |

## 附录 B · 开放问题（实现前需人类拍板）

1. **minSdk**（建议 26+）
2. **开源许可证**（MIT / Apache-2.0 二选一）
3. **沙箱发行版**（Alpine 轻量 vs Debian 生态全；首包体积策略）
4. **默认搜索后端**（免 key 兜底方案选型）
5. **中文字体配对**（MiSans / HarmonyOS Sans / 思源黑体）
6. **applicationId / 域名**（`quine.cn` 疑似可注册；GitHub org 建议 `quine-app` / `getquine` / `quinehq`）
7. **Rive 资产制作**（拟人化动效的美术产出路径）
8. **发布节奏**（先私有自用 or 直接公开开发）
9. **图标切换默认值**（浅色 / 深色 / 是否再加"跟随系统"第三选项）

## 附录 C · 人工狗粮验收清单（每里程碑跑一遍）

- [ ] 冷启动 → 首屏可交互 < 2s（设备基准实测）
- [ ] 断网发消息 → 明确错误 + 重试按钮
- [ ] 杀掉进程再打开 → 会话 / 草稿 / 任务状态完整恢复
- [ ] 旋转 / 折叠 → 状态不丢（滚动位置、输入框内容）
- [ ] 危险操作（删除）→ 必出确认弹层（人话三段）
- [ ] 修改文件 → 快照生成 → 回滚成功
- [ ] 飞行模式跑沙箱命令 → 正常（证明纯本地）
- [ ] 全程无 Key / 内容明文出现在日志（抽查 logcat）
